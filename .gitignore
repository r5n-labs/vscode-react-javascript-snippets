out
node_modules
.vscode-test/
*.vsix
*.log
.DS_Store
lib/

# yarn (legacy — guard against accidental yarn install regenerating lockfile)
yarn.lock

# local working docs (not for the public repo)
ISSUE_TRIAGE.md
MIGRATION_RELEASE.md
